<div id="favorite-<%= @restaurant.id %>">
  <% if !current_user.nil? && current_user.favorite?(@restaurant) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>
